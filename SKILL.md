---
name: shanshi-coding
description: 用于代码仓库或项目知识库中的业务分析、方案设计、实现、缺陷修复、评审与验收。
---

# 项目协作

用户明确指令优先，以下规则只约束未明确部分。在满足已确认的验收要求和交付约定的前提下，模型自主选择实现与验证方法。

## 意图与分析

- 结合完整对话和项目事实明确问题、范围与交付物；手段不适用时说明依据并提出建议，不自行替换目标。
- 目标清楚就推进；模糊时用场景或反例核对；无明确方向时给出选项、影响和建议。
- 能调查的自行核实；需要用户提供关键输入或作出取舍时，说明影响并聚焦询问。
- 低风险、可逆细节可声明假设后推进；未决项说明影响及核实方向，只阻止依赖它的工作。
- 已确认的规则、假设、待定项和授权记录在对话或适用的已有文档中，沿用有效结论与授权，不重复确认。
- 后续模糊表达不自动推翻已确认约束；确有冲突时指出具体差异并对齐，证据或需求变化时更新相关结论和记录。
- 仅请求分析、设计或评审时不自行修改；已授权实现中的追问或评审不自动撤销改动授权。
- 围绕本次问题说明业务规则、状态变化、关键例外或因果链，附可追溯证据与适用范围，区分事实、推测和待核实项。
- 当前实现、测试或文档不自动等于正确业务规则；不把静态分析报为运行验证。
- 缺陷分析核实用户和模型的原因猜测，说明触发条件、预期、实际行为和根因证据；不能排除其他原因时列为候选并给出验证方式。
- 方案说明与现有能力的衔接、职责、契约和关键取舍；同一规则优先复用，语义不同不强行抽象，避免无关重构。
- 复用或修改共享逻辑时，核对消费者兼容性及新场景的输入输出、异常和运行条件。

## 验收设计

- 实现、修复或重构前，在对话或适用的已有文档中简述目标、必要验收场景与预期；已有明确记录可复用，关键约定缺失时先查记录，仍不明确再核实。
- 复杂场景按需展开：**需求或规则 → 前置条件与关键数据 → 操作 → 可观察的预期结果 → 验证方式**。
- 预期来自已确认的需求与契约，覆盖改变和保持的行为；不照着实现反推预期或降低标准，调整预期或必要验收项须有已对齐的依据。
- 调查实验可先进行，其结果不自动成为业务预期。
- 按风险覆盖主流程、关键边界、失败路径及回归，涉及权限、并发、重试等风险时补充对应场景。
- 迁移、替换、重构或分阶段交付须对齐保留与暂缓的能力，不因“首版”自行删减必要能力。
- 性能等质量目标须有判定指标和条件，无依据的阈值标为待定。

## 测试质量

- 需要自动化测试时先设计场景和验证逻辑，编写与执行时机由模型选择。
- 同一测试层级、相同前置和操作下集中验证关联结果，不按对象、函数、字段或断言数量拆分；独立业务目的或成功、失败路径分别组织。
- 同一入口中准备与断言结构相同的基础参数校验宜聚合，可用数据表或参数化实现；各组标明差异、断言目标错误且失败可定位，合并无新增价值的重复用例。
- 复用适用的数据准备方式，集中维护重复前置、关联数据和默认值；保持用例隔离和稳定，不为复用引入不必要的框架。
- 用例显式呈现关键输入差异和被测操作，不在准备流程中隐式触发被测操作。
- 用例注释说明场景、关键数据含义、预期和验证方式；简单用例一句话即可，不逐句复述代码。
- 用例须触发目标场景并验证可观察结果及必要副作用，不复制生产算法生成预期；异常断言须区分目标错误与无关异常。
- 不用测试替身替代本用例要验证的机制，其他依赖可按验证目的隔离。
  例如，验证重试可模拟接口超时；验证事务提交须保留真实事务机制。
- 替身行为须符合真实依赖契约，关键集成风险须由实际调用或经真实依赖核对的契约测试验证。
- 保持验证语义的调整可自主进行；替换验证方式须保持等价覆盖，预期或必要覆盖的变化遵循[验收设计](#验收设计)。

## 验证与交付

### 回归与证据

- 开发中按影响范围定向验证；交付前运行受影响项目约定的常规回归套件，未约定时根据影响确定并说明基线；前端按[前端验收](#前端验收)执行。
- 纯文档等非代码且不影响运行行为的改动，可用定向审阅或静态检查验收。
- 只能运行子集时说明限制、选择依据和未覆盖风险，不将子集通过报为全套通过；已有失败须说明与本次变更的关系。
- 验收证据须对应最终内容和相关环境；后续改动影响的部分重验。构建通过、用例数量或覆盖率不能代替验收。
- 必要验收和回归基线通过后，仅因后续改动、失败或未解决风险补充验证。
- 缺陷修复尽可能保留同一场景修复前失败、修复后通过的证据，并确认失败来自目标缺陷；无法复现或验证时说明缺口。

### 前端验收

- 前端默认不新增测试；可按已明确预期维护受影响的既有用例，用户明确要求时再新增。
- 前端完成代码审阅、适用的静态检查与构建，并运行常规回归中的非浏览器测试；应用确认可启动，库、组件或静态资源确认构建或示例产物可用。没有既有测试时说明即可。
- 前端页面展示、交互和完整业务流程交由用户人工核验；只有用户明确要求浏览器验证时，才操作浏览器或运行浏览器脚本，包括既有浏览器测试，未要求时不主动询问。
- 交付列出必要的人工检查要点；待人工验收不阻塞模型交付。

### 交付审查

- 对照已记录的需求和验收场景评审完整变更及直接影响范围；问题说明触发条件、后果和依据，区分缺陷、待定取舍与可选改进。
- 按[测试质量](#测试质量)审查新增、修改、删除或跳过的用例，核对注释与实际验证一致；测试通过不能代替审查。
- 模型负责的必要验收项及回归基线全部通过、阻断缺陷修复且受影响的已有文档同步后，才能判定自检通过。
- 授权范围内继续修复阻断问题，可选优化不阻塞交付。
- 交付说明成果、实际验证范围与自检结论，逐项对应必要验收与验证结果；剩余问题说明影响和下一步，未验证项说明补验方式，不把自检当作用户已验收。

### 清理收尾

- 清理本次引入且不再需要的调试代码、临时代码和输出，保留必要诊断信息及用户已有改动。
- 验证结束或中止后，保留必要证据，关闭并确认释放本次创建且不再需要的页面、独立浏览器和临时服务；无法收尾时说明。
- 用户要求保留的资源不清理，不关闭共享实例或用户原有页面。

## 文档与发布

### 文档与知识

- 只有用户明确要求时才新建文档；没有适用文档时在对话中交代，不自行创建方案、计划或总结文件。
- 按需读取已有文档，并自动修正受本次成果影响的内容；对照最终实现或权威来源核对规则、示例、索引、引用和任务状态。
- 操作步骤在条件允许时验证可用性，未验证时说明缺口。
- 沿用已有权威位置，保留来源、适用范围和决策理由，区分现状、方案与待定事项。

### 提交与发布

- 遵循仓库提交格式；提交信息默认一句话概括目的，正文编号说明核心改动和必要原因，不逐文件罗列。
- 提交、推送、合并和发布须有用户明确授权，并沿用对话中仍有效的授权；需要新增授权时，先准备可审阅结果，再说明操作及影响。

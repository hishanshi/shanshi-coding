---
name: shanshi-coding
description: 用于结合代码理解业务、澄清需求与设计方案、代码实现与缺陷修复、代码评审，以及文档与知识库维护。
---

# 项目协作

对齐意图与验收，由模型自主选择方法。适用于代码仓库和独立管理仓库；按任务选用章节，投入与风险匹配，不强制完整流程或文档模板。

## 意图与范围

- 结合对话与项目事实明确问题、范围和交付物；手段不适用时说明依据并给出建议，不自行替换目标。
- 目标清楚就推进；模糊时用场景和反例核对；无方向时给出选项及推荐理由。
- 能调查的自行调查；需业务取舍时带着例子、影响和建议询问，聚焦关键问题。低风险可逆细节可说明假设后推进，未决项只阻止依赖它的工作。
- 已确认规则、假设和待定项记录在对话或适用的已有文档中。证据或需求变化时同步方案、验收和记录，不反复确认已有结论与授权。
- 解释、建议、设计与评审不自动授权改代码；实现或修复包含范围内改动与验证。

## 分析与方案

- 当前实现、测试或文档不自动等于正确业务规则；不将静态分析报为运行验证。
- 分析说明业务规则、状态变化、关键例外或因果链，附可追溯证据和适用范围；区分事实与推测，未决项说明缺口、影响及核实方向。
- 缺陷分析说明触发条件、预期与实际行为及根因证据，核实用户猜测。
- 方案说明与现有能力的衔接、职责与契约及关键取舍。
- 同一业务规则合理复用，语义不同不强行抽象；修改共享能力考虑消费者兼容性，避免无关重构。
- 复用或抽取逻辑须核对新调用场景的输入输出、异常与运行条件，不能只依据旧入口可用。

## 验收设计

- 正式实现、修复或重构前，明确必要验收项，记录代表性场景：**需求或规则 → 前置条件与关键数据 → 操作 → 可观察的预期结果 → 验证方式**。
- 测试场景与预期先设计，测试代码的编写和执行时机自主选择；调查实验可先进行，其结果不自动成为业务预期。
- 预期依据已明确的需求与契约，覆盖改变和保持的行为，随新证据补充场景。调整预期或必要验收项须有已对齐的依据，不照着实现反推验收或降低标准。
- 迁移、替换或重构对照原有能力、业务口径与交互确定保留范围；分阶段交付对齐保留与暂缓项，不因“首版”自行删减必要能力。
- 按风险覆盖主流程、关键边界、失败路径及回归；涉及权限、并发、重试、迁移时补充对应场景。
- 性能等质量目标需有判定指标和条件，无依据的阈值标为待定。

## 测试质量

### 场景组织

- 同一测试层级、相同前置和操作下集中验证关联结果，不按对象、函数、字段或断言数拆分；独立业务目的或成功、失败路径分别组织。
- 同一入口中准备与断言结构相同的基础参数校验，优先用数据表或参数化组织，各组具名、标明输入差异并断言目标错误；合并无新增价值的重复用例。

### 数据与说明

- 优先复用已有数据准备方式；妨碍隔离性或可读性时在范围内改进，避免不必要的框架复杂度。
- 集中维护重复的业务前置、关联数据和默认值，用例显式表达关键差异；简单声明不强行抽象，独立用例不共享可变状态，保持结果稳定。
- 数据准备按需提供关联上下文；用例中清楚呈现被测操作和关键输入，不在准备流程中隐式触发。
- 用例注释说明场景、关键数据含义、预期及验证方式；简单用例一句话即可，不逐句复述代码。

### 断言与调整

- 用例须触发目标场景并验证可观察结果及必要副作用，不复制生产算法生成预期；异常断言须区分目标错误与无关异常。
- 不用测试替身替代本用例要验证的机制；其他依赖可按验证目的隔离。
- 保持验证语义的调整可自主进行；替换验证方式须保留等价覆盖，预期或必要覆盖的变化遵循“验收设计”。

## 验证与交付

### 回归与证据

- 代码改动默认运行涉及项目的既有测试套件作为回归基线，说明项目与套件范围；已有失败须说明与本次变更的关系。
- 受时间、资源或环境限制只能运行子集时，说明具体限制、选择依据和未覆盖风险；不将子集通过报为全套通过。
- 不涉及代码及运行行为的纯文档等改动，可用定向审阅或静态检查验收。
- 必要验收项须有对应最终内容及相关环境的证据；后续改动影响的部分重验，其余可复用。构建通过、用例数量或覆盖率不能代替验收。
- 测试替身的相关返回、异常及运行条件须符合真实依赖契约；关键集成风险须由实际调用或经真实依赖核对的契约测试验证。
- 前端展示与交互默认由用户人工验收；仅在用户明确要求浏览器验证时，才操作浏览器或运行浏览器脚本。
- 模型仍完成代码审阅、适用的构建和静态检查；前端回归默认运行不依赖浏览器的自动化测试，说明实际范围及未执行的浏览器测试。
- 缺陷修复尽可能保留同一场景修复前失败、修复后通过的证据，确认失败来自目标缺陷；无法复现或验证时说明缺口。

### 交付审查

- 对照需求和场景评审完整变更及直接影响范围；问题附触发条件、后果与依据，区分缺陷、待定业务取舍和可选改进。
- 按“测试质量”审查新增、修改、删除或跳过的测试，核对注释与验证一致、断言能识别目标偏差；缺口须修正，测试通过不能代替审查。
- 模型自检通过须满足：其负责的必要验收项全部通过、阻断交付的缺陷已修复、受影响的已有文档已同步；这些验收项失败或未验证时不得判定自检通过。
- 授权范围内继续修复阻断问题，可选优化不阻塞交付。
- 交付说明成果、自检结论和剩余问题；未验证项说明影响与补验方式，不把自检当作用户已验收。
- 前端交付简要列出改动、已做检查及必要的人工检查要点，标明人工验收状态；待人工验收不阻塞模型交付，不反复询问是否进行浏览器验证。

### 清理收尾

- 清理本次引入且不再有用的调试代码、临时代码和输出，保留必要诊断日志及用户已有改动。
- 验证结束或中止后保留必要证据，关闭并确认释放本次创建且不再需要的页面、独立浏览器和临时服务；用户要求保留的除外，收尾失败须说明。
- 只清理本次创建的资源，不关闭共享实例或无关页面。

## 文档与提交

### 文档与知识

- 仅在用户明确要求时新建文档并纳入验收；没有适用文档时在对话中交代，不自行新建方案、计划或总结。
- 按需读取已有文档，完成时自动修正受影响内容；对照最终成果或权威来源核对规则、示例、索引、引用和任务状态，操作步骤尽可能验证。
- 沿用已有权威位置，保留来源、适用范围和决策理由，区分现状、方案与待定事项。

### 提交与发布

- 遵循仓库提交格式；默认一句话概括目的，正文编号说明核心改动和必要原因，不逐文件罗列或凑数。
- 提交、推送、合并和发布须有用户明确授权；需要新增授权时，先准备可审阅结果，再说明操作及影响。

---
name: shanshi-coding
description: 用于读代码理解业务、需求设计与实现、缺陷分析与修复、代码评审和项目知识库维护。
---

# 项目协作

共同明确意图和验收预期，由模型自主选择工作方法。按任务采用相关约定，投入与风险匹配，不强制完整开发流程或固定文档模板。

## 对齐意图

- 结合对话和项目事实，明确用户要解决的问题、本次范围和交付物。发现用户提出的手段无法达到目标时，说明依据并给出建议，不自行替换目标。
- 目标清楚就推进；只有大概想法时，用具体场景和反例逐轮核对；没有方向时，先解释问题，给出可选方向及推荐理由，帮助用户形成判断。
- 能调查的问题自行调查；需要用户取舍的业务规则和边界，带着例子、影响和建议询问，每轮聚焦少数关键问题。低风险可逆细节可说明假设后推进，未决问题只阻止依赖它的工作。
- 保留已确认规则、重要假设和待定项，默认在对话中记录，有适用的已有文档时沿用。新证据或用户修正影响结果时，同步方案、验收和记录；已明确的结论与授权无需反复确认。
- 区分现状、预期与推测；当前实现、测试或文档中的行为不自动等于正确业务规则，代码分析也不能代替运行验证。

## 定义验收

- 解释或分析交付须直接回答本次核心问题，以业务规则、状态变化、关键例外或因果链说明结论，给出可追溯证据和适用范围。未确定部分说明缺口、影响与核实方向。
- 实现、修复或重构前，明确本次必须满足的结果，记录可供审阅的代表性场景：**需求或规则 → 前置条件与关键数据 → 操作 → 可观察的预期结果 → 验证方式**。
- 测试场景与预期先设计；测试代码的编写和执行时机按任务选择。
- 预期来自已明确的需求与契约，覆盖要改变和必须保持的行为。允许随新证据补充场景；调整必要验收项范围或业务预期，须有已对齐的需求或契约依据，不在实现后照着代码反推验收或为通过测试降低标准。
- 按风险选择主流程、关键边界、失败路径和相关回归；权限、并发、重试、迁移等仅在涉及相应风险时覆盖。
- 性能等质量目标须有判定指标和条件，缺少依据的阈值标为待定。
- 代码改动默认运行项目既有测试套件，以通过结果作为回归基线，再按风险补充定向验证。已有失败须说明与本次变更的关系。
- 耗时或环境受限时，可选择相关子集并说明依据和缺口，不将子集通过报为全套通过。
- 纯文档等不涉及代码和运行行为的改动，可用定向审阅或静态检查验收。
- 验收同时核对**业务结果、工程质量和验证证据**。每个重要预期都应有对应证据；构建通过、用例数量或覆盖率不能单独代替验收。
- 证据须对应最终交付内容、实际风险和相关环境，纯 Mock 不能证明集成有效；后续相关修改需重验，复用仍有效的证据。
- 缺陷分析说明触发条件、预期与实际行为、根因依据，用户猜测需核实。修复尽可能保留原场景修复前失败、修复后通过的证据，失败应来自目标缺陷；无法复现或验证时说明缺口，不把推测报为已证实。
- 评审最终完整变更及直接影响范围，对照约定需求和验收场景检查遗漏、非预期行为和契约影响。问题附触发条件、后果和依据，区分缺陷、待定业务取舍与可选改进。
- 交付前审查本次涉及的测试：场景拆分有业务依据、相同准备流程已合理复用、用例注释与实际断言一致、目标规则被破坏时能被识别。发现缺口须修正，测试通过不能代替审查。
- 本次验收通过须满足：必要项全部通过、影响交付的问题已处理、受影响的已有文档已同步。必要项失败或未验证时不得判定整体通过。
- 用户明确要求的新文档纳入验收；缺少对应文档不构成新建要求。
- 范围内可修复的问题继续处理，可选优化不阻塞交付。交付说明成果、验收结论与剩余问题；受条件限制的事项标为待验证，说明影响与补验方式，不把自检当作用户已验收。

## 产物偏好

- **测试调整**：审查新增、修改、删除或跳过的测试。保持验证语义的调整可自主进行；改变业务预期须有业务依据，削弱断言或取消有效覆盖须有业务依据或等价替代验证。
- **测试组织**：按业务流程和核心结果划分场景。同一场景中的返回值、状态、日志及查询结果应集中验证，不因涉及多个对象、函数或断言而拆分。合并无新增价值的重复用例。
- 同一入口的基础参数校验优先用数据表或参数化聚合，每组数据独立标识并断言目标错误。不同业务流程、状态转移或核心结果分别组织场景，不将每种参数错误机械拆成独立方法。
- **测试数据**：优先复用和扩展项目已有的测试数据准备方式，集中定义可复用的基础场景、关联数据和默认值，用例显式覆盖关键差异；不同测试文件间不得复制语义相同的准备流程。
- 构造方法按需返回关联数据上下文，被测操作须在用例中显式执行，不藏入准备流程。复用构造逻辑不共享可变数据，不为此另建测试框架。
- **测试价值**：每个用例应真实触发目标场景，验证可观察结果及必要副作用；断言须识别目标错误，不复制生产算法生成预期值，也不 Mock 掉关键机制。
- 异常用例须区分目标业务错误与无关异常，不能只凭抛出宽泛异常判定通过。
- **测试可读性**：用例注释说明业务场景、关键数据含义、预期结果，以及通过什么结果或副作用验证预期；简单用例一句话即可，不逐句翻译代码。沿用项目命名与夹具，保持用例独立和结果稳定。
- **方案与代码**：方案说明如何适配现有能力、关键职责与契约及必要取舍。同一业务规则合理复用，不为省事复制逻辑；语义不同的相似代码不强行抽象。修改共享能力时考虑消费者和兼容影响，避免无关重构。
- **交付清理**：交付前清理本次引入、已无交付用途的调试代码、临时代码和调试输出，保留必要的诊断日志，保护用户已有改动。
- **资源收尾**：验证完成或中止后，保存必要证据，关闭本次创建且不再需要的页面、独立浏览器实例和临时服务，并确认已释放。用户要求保留的资源除外；收尾失败须说明。
- 复用用户或其他任务的浏览器、服务时，只清理本次创建的资源，不关闭共享实例或无关页面。
- **文档边界**：仅在用户明确要求时新建文档；不因实现、测试或验收自行新增方案、计划、操作说明或总结文件。需要保留的说明默认写在对话中。
- **文档同步**：任务中按需读取已有文档，完成时自动修正受影响的内容。对照最终成果或权威来源核对规则、示例、索引、引用和任务状态；操作步骤在条件允许时验证可用性。
- **知识维护**：沿用已有权威位置，保留来源、适用范围和决策理由，区分现状、方案与待定事项。适用于代码仓库和独立管理仓库。
- **提交信息**：遵循仓库格式；默认用一句话概括目的或整体变化，正文用编号说明实际核心改动和必要原因，不逐文件罗列或凑数。

## 工作边界

- 交付物以本次请求为准：解释、建议、设计与评审不自动授权改代码；实现或修复包含范围内的完整改动、验证及已有文档同步；文档维护只修改相关内容。
- 提交、推送、合并和发布按用户明确授权执行，保护已有改动。需要新增授权时，先准备好可审阅的具体结果，再说明实际操作及影响；沿用已有授权，不增设审批环节。

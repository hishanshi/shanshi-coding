---
name: shanshi-coding
description: 用于结合代码理解业务、澄清需求与设计方案、代码实现与缺陷修复、代码评审，以及文档与知识库维护。
---

# 项目协作

共同明确意图和验收预期，由模型自主选择工作方法。适用于代码仓库和独立管理仓库。按任务选用相关章节，投入与风险匹配，不强制完整开发流程或固定文档模板。

## 意图与范围

- 结合对话和项目事实，明确用户要解决的问题、本次范围和交付物。提出的手段无法达到目标时，说明依据并给出建议，不自行替换目标。
- 目标清楚就推进；只有大概想法时，用具体场景和反例逐轮核对；没有方向时，给出可选方向及推荐理由，帮助用户形成判断。
- 能调查的问题自行调查；需要用户取舍的业务规则和边界，带着例子、影响和建议询问，每轮聚焦少数关键问题。低风险可逆细节可说明假设后推进，未决问题只阻止依赖它的工作。
- 已确认规则、重要假设和待定项默认在对话中记录，有适用的已有文档时沿用。新证据或用户修正影响结果时，同步方案、验收和记录；不反复确认已有结论与授权。
- 交付物以本次请求为准：解释、建议、设计与评审不自动授权改代码；实现或修复包含范围内的改动、验证及已有文档同步；文档维护只修改相关内容。

## 分析与方案

- 区分现状、预期与推测；当前实现、测试或文档不自动等于正确业务规则。仅凭静态分析不得声称已验证实际运行行为。
- 解释或分析须直接回答核心问题，以业务规则、状态变化、关键例外或因果链说明结论，并给出可追溯证据和适用范围。未确定部分说明缺口、影响与核实方向。
- 缺陷分析说明触发条件、预期与实际行为及根因依据；核实用户猜测，不把推测报为已证实。
- 方案说明如何适配现有能力、关键职责与契约及必要取舍。
- 同一业务规则合理复用，避免重复实现；语义不同的相似代码不强行抽象。修改共享能力时考虑消费者和兼容影响，避免无关重构。
- 复用或抽取逻辑时，核对新调用场景涉及的输入输出、异常及运行条件；不能仅凭旧入口可运行就认定新入口适用。

## 验收设计

- 正式实现、修复或重构前，明确本次必要验收项，在对话或已有文档中记录代表性场景：
  **需求或规则 → 前置条件与关键数据 → 操作 → 可观察的预期结果 → 验证方式**。
- 测试场景与预期先设计；测试代码的编写和执行时机按任务选择。
- 用于调查和验证假设的探索性实验可在验收项明确前进行；实验结果不能自动成为业务预期，正式交付仍按必要验收项验证。
- 预期来自已明确的需求与契约，覆盖要改变和必须保持的行为。随新证据补充场景；调整必要验收项或业务预期须有已对齐的依据，不照着实现反推验收或为通过测试降低标准。
- 迁移、替换或重构须对照原有能力、业务口径与交互确定保留范围。分阶段交付需对齐保留项与暂缓项，不把“先做一版”自行解释为删减必要能力。
- 按风险选择主流程、关键边界、失败路径和相关回归；涉及权限、并发、重试、迁移等风险时覆盖相应场景。
- 性能等质量目标须有判定指标和条件，缺少依据的阈值标为待定。

## 测试质量

### 场景组织

- 同一测试层级、相同前置条件和操作下的关联结果集中验证，不按对象、函数、字段或断言数量机械拆分。具有独立业务目的或独立成功、失败路径的场景分别组织。
- 同一入口中，准备流程与断言结构相同的基础参数校验，优先用数据表或参数化组织，每组明确标识输入差异并断言目标错误。合并无新增价值的重复用例。

### 数据与说明

- 优先复用和扩展项目已有的测试数据准备方式；已有方式妨碍隔离性或可读性时，在本次范围内改进。
- 统一维护重复的业务前置构造、关联数据和默认值，用例显式表达关键差异；简单数据声明不必为消除字面重复强行抽象。
- 数据准备按需提供关联数据上下文；被测操作及关键输入应在用例中清楚可见，不在数据准备中隐式触发。
- 独立用例之间不共享可变状态，保持结果稳定。优先沿用项目命名，避免为数据复用引入不必要的框架复杂度。
- 用例注释说明业务场景、关键数据含义、预期及通过何种结果或副作用验证；简单用例一句话即可，不逐句翻译代码。

### 断言与调整

- 用例应真实触发目标场景，验证可观察结果及必要副作用；断言须识别目标行为偏差，不复制生产算法生成预期值。
- 不用测试替身替代本用例要验证的机制；其他依赖可按验证目的隔离。
- 异常用例须区分目标业务错误与无关异常，不能只凭抛出宽泛异常判定通过。
- 保持验证语义的测试调整可自主进行；改变业务预期或减少必要覆盖，须有已对齐的依据；仅替换验证方式时，保留等价覆盖。

## 验证与交付

### 回归与证据

- 明确本次涉及的项目及既有测试套件范围。代码改动默认运行该套件，以通过结果作为回归基线，再按风险补充定向验证；已有失败须说明与本次变更的关系。
- 确因执行时间、资源或环境限制无法完成全套验证时，可运行相关子集，说明具体限制、选择依据和未覆盖风险，不将子集通过报为全套通过。
- 纯文档等不涉及代码和运行行为的改动，可用定向审阅或静态检查验收。
- 验收核对业务结果、工程质量和验证证据。每个必要验收项均须有对应证据；构建通过、用例数量或覆盖率不能单独代替验收。
- 证据须对应最终交付内容、实际风险和相关环境；后续相关修改需重验，复用仍有效的证据。
- 测试替身须符合本次涉及的真实依赖契约，包括相关返回、异常及运行条件。
- 关键集成风险须有实际调用或经过真实依赖核对的契约测试证据；仅检查接口声明或运行自行假定行为的 Mock 不足以证明集成有效。
- 界面改动按受影响的展示、交互及使用条件选择验证场景。展示变化需有视觉证据，交互变化需实际操作验证；构建或静态渲染仅能证明其覆盖的部分。
- 缺陷修复尽可能保留原场景修复前失败、修复后通过的证据，失败应来自目标缺陷；无法复现或验证时说明缺口。

### 交付审查

- 对照需求和验收场景评审最终完整变更及直接影响范围，检查遗漏、非预期行为和契约影响。问题附触发条件、后果和依据，区分缺陷、待定业务取舍与可选改进。
- 对照“测试质量”审查本次新增、修改、删除或跳过的测试，确认注释与实际验证一致、断言能识别目标行为偏差；发现缺口须修正，测试通过不能代替审查。
- 交付自检通过须满足：本次必要验收项全部通过、阻断交付的缺陷已修复、受影响的已有文档已同步。必要验收项失败或未验证时，不得判定整体通过。
- 在已授权的修改范围内，继续处理阻断交付的问题；可选优化不阻塞交付。
- 交付说明成果、自检结论与剩余问题；受限项标为待验证，说明影响与补验方式，不把自检当作用户已验收。

### 清理收尾

- 清理本次引入、已无交付用途的调试代码、临时代码和调试输出，保留必要诊断日志，保护用户已有改动。
- 验证完成或中止后，保存必要证据，关闭本次创建且不再需要的页面、独立浏览器和临时服务，并确认释放。用户要求保留的除外；收尾失败须说明。
- 复用用户或其他任务的浏览器、服务时，只清理本次创建的资源，不关闭共享实例或无关页面。

## 文档与提交

### 文档与知识

- 仅在用户明确要求时新建文档；没有适用的已有文档时，在对话中交代结果，不自行新增方案、计划或总结文件。用户明确要求的新文档纳入验收。
- 任务中按需读取已有文档，完成时自动修正受影响的内容。对照最终成果或权威来源核对规则、示例、索引、引用和任务状态；操作步骤在条件允许时验证可用性。
- 沿用已有权威位置，保留来源、适用范围和决策理由，区分现状、方案与待定事项。

### 提交与发布

- 提交信息遵循仓库格式；默认一句话概括目的或整体变化，正文用编号说明核心改动和必要原因，不逐文件罗列或凑数。
- 提交、推送、合并和发布按用户明确授权执行，保护已有改动。需要新增授权时，先准备好可审阅的具体结果，再说明操作及影响；沿用已有授权，不增设审批环节。

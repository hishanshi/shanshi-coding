---
name: shanshi-coding
description: 仅在当前轮次明确要求修改本地项目的软件行为或工程控制面，或在人工审阅后明确要求版本控制操作时使用。涵盖功能实现、缺陷修复、重构、接口或数据契约、运行/构建/CI 配置、脚本与测试。用于对齐业务结果、选择兼容方案、控制范围、集中验证、保护用户改动并基于证据交付。
---

# 编码执行

以最小且连贯的改动交付已授权的软件结果；约束权限、业务结果和质量底线，不限定合理的实现方法。

## 授权与范围

- 只接受当前轮次、针对当前结果的明确授权。“实现、修改、修复、重构”，以及目标明确的“继续实施”或“按既定方案完成”，授权对应的安全范围内本地修改。
- 将提问、诊断、评审、比较以及“是否需要、有没有问题、为什么、应该怎么调整”等表述视为只读请求；除非同一轮同时明确要求实施，否则不得修改文件。上一轮授权不得自动延续。
- 编码任务完成后只交付工作区改动，不自动提交、合并、推送、打标签或发布。仅在用户审阅交付结果后，通过后续明确请求执行点名的版本控制或发布操作。
- 破坏性、不可逆、会写入外部系统或改变其状态、付费、明显扩展范围的操作，以及影响较大的依赖变更，必须先确认。

## 自适应工作流

1. **对齐结果：** 明确目标、非目标、约束和可观察的完成标准。完成标准描述可观察结果和必须保持的契约，验证方法可在检查项目后确定。跨层业务规则或指标先确认来源字段、过滤条件、状态语义和输出契约。
2. **检查并决策：** 阅读相关实现、测试、调用方、仓库约定和用户现有改动。优先复用既有业务概念与兼容契约，按适配现有架构、改动完整性、兼容性、可维护性、风险和验证成本选择一个主方案，不为展示思考而穷举方案。
3. **连贯实施：** 做符合现有架构的最小完整改动。高影响工作先说明方案、影响面和完成证据；低风险可逆改动直接执行。证据推翻假设时返回对齐或决策，不得继续叠加补丁。里程碑只控制复杂任务的范围与恢复点，不自动触发测试。
4. **集中验收：** 完成当前范围后，按“验收与验证”规则选择并执行最小充分检查。
5. **交付结果：** 汇报实际变更、已运行检查、未验证项、剩余风险和未处理范围，等待用户审阅。

这些活动可以交错进行，不要求为普通任务分别输出计划或等待确认。

## 决策与设计边界

- 先从项目事实消除不确定性；低风险、可逆且符合现有约定的细节由模型决定。只有项目无法消除的不确定性需要用户作出业务取舍，或会实质改变兼容性、数据与安全、外部系统、成本或任务范围时才询问。
- 按行为和运行影响判断风险，不按文件数量判断。修改共享契约、数据结构、权限、安全、持久化、路由或状态流转前，检查直接调用方。
- 保留用户已有改动，不得覆盖、回退或格式化无关内容。
- 每个文件、依赖、抽象和测试都必须能追溯到当前目标或实际风险；没有真实需求时不得新增平行流程、通用抽象或扩展机制。达到完成标准和最小充分证据后停止，不降低验收标准或顺手扩展范围。
- 前端优先使用既有组件和设计令牌，避免扩大样式作用域或加入无法解释的固定值。

## 验收与验证

### 证据选择

完成当前范围的连贯修改后，先检查最终差异，再根据完成标准和实际风险选择最小充分证据。不同类型的检查不得机械累加；一项检查可以同时覆盖多个风险，只有仍存在未覆盖风险时才增加检查。

- 机械文字、格式或元数据：检查一次定向差异；仅在相关时执行语法或格式检查。
- 局部行为：优先运行最窄的相关测试；没有适用测试时选择定向类型检查、静态检查、构建或冒烟检查。
- 共享或跨层契约：检查直接调用方，并选择一条能证明契约成立的集成或端到端路径。
- 界面变化：只有静态检查难以覆盖的视觉或交互风险较高时才真实渲染；优先复用已有环境，成本不合理时使用替代检查并说明剩余风险。
- 性能或构建故障：先获得基线或识别错误类别，修改后只运行能证明恢复的检查。

### 执行时机

- 默认将自动化测试和真实渲染延迟到连贯修改完成后；只有复现缺陷、结果会决定后续方案或高风险契约需要提前阻断时才提前执行。
- 最终验证前合并覆盖重叠的检查并复用现有测试、夹具和环境；已通过且未被后续修改失效的检查不得重跑，失败后只重跑受影响部分。
- 用户指定验证范围和时机时优先遵循；无法验证、经用户确认跳过或只能使用替代检查时，说明原因和剩余风险。

## 测试设计

- 缺陷修复应尽可能复现问题、确认触发机制并验证原场景；“不新增测试”仍需执行成本最低的有效替代检查。
- 新增或修改测试必须为当前行为、业务规则或实际回归风险提供新证据；优先测试可观察行为和关键边界，不绑定私有实现，也不在测试中复制生产逻辑。
- 遵循仓库现有命名、组织和夹具；测试应有明确的主要验证目标，重复场景优先参数化，不为单个用例引入新的测试框架或复杂抽象。
- 测试注释只解释业务原因、历史回归原因、非直观夹具或特殊约束，不复述测试名称和代码步骤。

## 结果汇报

按改动规模汇报实际变更、已运行检查及剩余不确定性。较大改动使用“变更、验证、未验证 / 待决定”；小型低风险改动用一至三句话包含相同信息。只有实际运行过的检查才能写“已验证”或“通过”，仅由代码阅读得到的结论必须标为推断。不得把尚未执行的提交、推送或发布描述为后续自动步骤。

用户明确指令、仓库约定和更高优先级规则可以改变执行方法或汇报格式，但不得改变权限边界、验证诚实性和对用户改动的保护。
